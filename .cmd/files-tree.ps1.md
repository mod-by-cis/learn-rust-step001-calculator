# files-tree.ps1

Jeśli system blokuje skrypty, odblokuj je na tę jedną sesję:

```PowerShell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

Uruchom skrypt:

```PowerShell
./files-tree.ps1
```
